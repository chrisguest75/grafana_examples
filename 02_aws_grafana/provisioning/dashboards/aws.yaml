apiVersion: 1

providers:
  - name: 'AWS'
    folder: 'AWS'
    orgId: 1
    folderUid: ''
    type: file
    allowUiUpdates: false
    updateIntervalSeconds: 60
    options:
      path: /etc/grafana/provisioning/dashboards/aws